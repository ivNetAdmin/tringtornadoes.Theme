@{
    /* Global includes for the theme
    ***************************************************************/

    SetMeta("charset", "utf-8");
    SetMeta("X-UA-Compatible", "IE=edge,chrome=1");
    SetMeta("viewport", "width=device-width, initial-scale=1.0");

    Style.Include("bootstrap.min.css").AtHead();
    Style.Include("modern-business.css");

    /* Inserting some ad hoc shapes
    ***************************************************************/

    // WorkContext.Layout.Header.Add(New.Branding(), "5"); // Site name and link to the home page
    //WorkContext.Layout.Footer.Add(New.BadgeOfHonor(), "5"); // Powered by Orchard
    WorkContext.Layout.Footer.Add(New.User(), "10"); // Login and dashboard links

}

@if (Model.Navigation != null)
{
    @Display(Model.Navigation)
}

@if (Model.Header != null)
{
    @Display(Model.Header)
}

@*<!-- Header Carousel -->
<header id="myCarousel" class="carousel slide">
    <!-- Indicators -->
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner">
        <div class="item active">
            <div class="fill" style="background-image:url('/media/default/carousel%20images/homepage/football.jpg');"></div>
            <div class="carousel-caption">
                <h2>Football</h2>
            </div>
        </div>
        <div class="item">
            <div class="fill" style="background-image:url('/media/default/carousel%20images/homepage/netball.jpg');"></div>
            <div class="carousel-caption">
                <h2>Netball</h2>
            </div>
        </div>
    </div>

    <!-- Controls -->
    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
        <span class="icon-prev"></span>
    </a>
    <a class="right carousel-control" href="#myCarousel" data-slide="next">
        <span class="icon-next"></span>
    </a>
</header>*@


    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                @Display(Model.Footer)
            </div>
        </div>
    </div>
